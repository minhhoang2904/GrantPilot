# Copy file này thành .streamlit/secrets.toml và điền giá trị thật.
# KHÔNG commit secrets.toml (đã có trong .gitignore).

SERVER_B_URL = "http://localhost:8001"
SERVER_C_URL = "http://localhost:8002"

# Tuỳ chọn, dùng khi answer_gen.py chuyển sang gọi LLM thật
OPENAI_API_KEY = "sk-..."
